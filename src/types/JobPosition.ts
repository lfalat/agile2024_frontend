type JobPosition = {
    levels: Array<[string, string]>;
    organizations: Array<[string, string]>;
    name: string;
    code: string;
    id: string;
    lastEdited: string;
    created: string;
    archived: boolean;
}

export default JobPosition